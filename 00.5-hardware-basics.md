# 第 0.5 章 · 硬件零基础生存手册（ZET6 版）

这一章不写外设代码。先认识开发板上的电源、GND、SWD 和串口，学会用万用表确认电压，再把 ST-Link 和 USB-TTL 安全接上。

本书固定使用 STM32F103ZET6，SPL 宏为 `STM32F10X_HD`，芯片有 512 KB Flash 和 64 KB SRAM。第一次接触硬件的话，完成这一章后至少要能指出开发板的 3.3V、GND、SWDIO、SWCLK，以及 USB-TTL 的 TX、RX、GND。

## 0.5.1 先认识电压、GND 和 GPIO

数字电路里，电压描述两个点之间的电势差。STM32F103 的数字 IO 主要工作在 3.3V 逻辑环境中，程序把某个 GPIO 配成高电平时，实际发生的是对应引脚电压发生变化。

GND 是电路的参考点。两个独立供电的模块通过 UART、SPI 等接口通信时，通常要把 GND 连在一起，否则双方看到的高低电平没有共同参考。后面遇到“串口有波形但全是乱码”“模块偶尔能收偶尔不能收”，共地就是首先要检查的项目之一。

GPIO 是 MCU 可以直接控制或交给外设使用的引脚。比如 PA9 可以配置成普通 GPIO，也可以配置成 USART1_TX。芯片手册告诉你 PA9 具备哪些功能，开发板原理图告诉你 PA9 在这块板上有没有接到其他器件。

还有一个基本安全规则：不要看到模块标着“5V”就把它的信号线接到 MCU。模块可能支持 5V 供电，但 TX、RX 或其他 IO 的输出电平需要单独确认。

## 0.5.2 确认你手里的芯片和开发板

本书工程固定按下面这些参数构建：

```text
芯片：STM32F103ZET6
内核：Cortex-M3
最高主频：72 MHz
Flash：512 KB
SRAM：64 KB
封装：LQFP144
SPL 宏：STM32F10X_HD
启动文件：startup_stm32f10x_hd.s
```

这些参数来自 MCU 型号。板载 LED 接在哪个引脚、按键怎么接、有没有 CH340、SPI Flash 的 CS 在哪里，则由开发板设计决定。

拿到一块新板后先看芯片丝印，再找这块板的原理图或厂家资料。把 LED、按键、USB 串口、外部晶振和板载 Flash 等实际连接填进 [板卡资源约定](./board-zet6-profile.md)。后面的章节都以这份记录为准。

C8T6、ZET6 都属于 STM32F103，但 Flash 容量、封装和芯片密度分类不同。其他教程里的 Medium Density 启动文件、64 KB Flash 链接脚本不能直接复制到本书工程。

## 0.5.3 万用表先会用两个档

刚开始只需要熟悉电压档和通断档。

测电压时，开发板需要上电。万用表切到直流电压档，黑表笔固定在已经确认的 GND，红表笔去测 3.3V、电源引脚或目标信号。测开发板 3.3V 电源时，读数应该接近 3.3V；具体允许范围以芯片和板卡电源器件的数据手册为准。

通断档用于确认两个点是否直接相连，比如判断排针上的 GND 是否和 USB 接口外壳附近的地相连。使用通断档或电阻档前先断电，不能在带电电路上测电阻。

测量时尽量让黑表笔固定在 GND，只移动红表笔。排针很密时尤其注意不要让表笔同时碰到相邻的 3.3V 和 GND，否则会直接短路电源。

## 0.5.4 第一次上电

第一次接板子时不要同时插满传感器、串口模块和显示屏。先只给开发板供电，观察电源指示灯和板子状态。

上电后做三件事：摸一下主要器件有没有快速发烫，闻一下有没有异常气味，再用万用表测 3.3V 对 GND 的电压。发现明显发热、异味或电压异常，立即断电检查供电极性和短路。

不要同时从多个来源给开发板供电，除非板卡原理图或说明明确支持这种接法。USB、ST-Link、外部 5V 和 3.3V 排针可能通过不同路径接入电源网络，具体能不能同时连接要看板卡设计。

## 0.5.5 接 ST-Link

SWD 调试最少需要这些信号：

```text
ST-Link SWDIO  ─── 开发板 SWDIO / PA13
ST-Link SWCLK  ─── 开发板 SWCLK / PA14
ST-Link GND    ─── 开发板 GND
```

目标板还要有正常供电。ST-Link 上的 3.3V 引脚是否参与供电，要看调试器型号和开发板供电方案；没有确认前，不要把它当成必须连接的线。

线接好以后先在 Linux 上确认 USB 设备：

```bash
lsusb
```

第 0 章安装 OpenOCD 后，还会继续验证 SWD 是否能真正读到 STM32。这里先做到调试器能被系统识别、SWDIO/SWCLK/GND 接线明确。

接线不要按杜邦线颜色判断。红线、黑线只是线材颜色，没有电气含义，最终以调试器标签和开发板丝印为准。

## 0.5.6 接 USB-TTL

后面 UART 章节会用 USB-TTL 看日志。最基本的三根线这样接：

```text
STM32 USART_TX  ───→ USB-TTL RX
STM32 USART_RX  ←─── USB-TTL TX
STM32 GND       ───── USB-TTL GND
```

TX 接对方 RX，RX 接对方 TX，两边 GND 相连。这里没有规定具体 USART 引脚，实际使用哪一路串口以后面的章节和 [板卡资源约定](./board-zet6-profile.md) 为准。

接 USB-TTL 前先确认它的逻辑电平。有些模块带 3.3V / 5V 跳线，有些供电电压和 TX 输出电平并不是一回事。需要时可以让 USB-TTL 单独接电脑，用万用表测 TX 空闲电平；UART TX 空闲时通常为高电平。

串口以后没有输出时，先检查共地和 TX/RX。持续乱码再检查波特率、帧格式、MCU 时钟和 USB-TTL 电平。

## 0.5.7 面包板和杜邦线

杜邦线用于临时连接排针和面包板。常见的公对公、公对母、母对母只表示两端接头形式，电气上都是导线。

面包板内部有固定的连通结构。中间区域通常每组 5 个孔内部相连，中间沟槽两侧互不相通；两边的电源轨通常纵向相连，但有些面包板会在中间断开。第一次使用时可以断电后用万用表通断档确认。

不要根据外观猜某一排孔一定连通。面包板型号不同，电源轨是否断开也可能不同。

## 0.5.8 还需要哪些工具

第一阶段准备这些东西就够用：

- ST-Link 或 DAP-Link：烧录和 SWD 调试。
- 3.3V USB-TTL：查看 UART 输出，也可以给 MCU 发送串口数据。
- 万用表：测电源、电平和通断。
- 杜邦线：连接开发板和外部模块。
- 面包板：搭 LED、电阻和简单模块连接。

逻辑分析仪在调 UART、SPI、I2C 时很有用，示波器可以进一步看模拟波形、边沿和电源质量。前几章没有这两样工具也能继续。

## 0.5.9 第一次完整接线

先断电，在开发板上找到 3.3V、GND、SWDIO 和 SWCLK。找不到就查原理图，不要试着碰运气。

接好 ST-Link 的 SWDIO、SWCLK 和 GND，再按开发板说明给目标板供电。上电后用万用表确认 3.3V 电源正常，并观察板子有没有明显发热。

确认 SWD 接线后，再接 USB-TTL。先连 GND，然后把 STM32 TX 接到 USB-TTL RX、STM32 RX 接到 USB-TTL TX。USB-TTL 的逻辑电平必须先确认。

最后把实际板卡型号、供电方式、LED、按键和串口引脚写进 [板卡资源约定](./board-zet6-profile.md)。不知道的项目明确写“待确认”，后面查到原理图或实际测量后再补。

## 0.5.10 出问题先查哪里

OpenOCD 找不到芯片时，先确认目标板有没有供电，再查 GND、SWDIO、SWCLK、ST-Link 和 USB 权限。SWD 接错和程序代码没有关系，先把物理连接确认完。

烧录成功但程序没有预期行为时，再进入软件排查。第 0 章会用 GDB 分别检查 `Reset_Handler` 和 `main()`，这样可以判断问题发生在启动阶段还是 GPIO 配置阶段。

串口没有输出时，先看 TX/RX 是否交叉、GND 是否相连。能看到信号但持续乱码，再查两边波特率、8N1 配置和 MCU 时钟。

外部模块一工作开发板就复位，先拔掉模块，单独确认开发板恢复正常。随后查模块峰值电流、电源路径和共地情况；WiFi 等模块发送时的瞬时电流可能明显高于空闲状态，供电能力要按模块数据手册确认。

## 0.5.11 本章验收

进入第 0 章之前，完成下面这些检查：

- [ ] 确认芯片是 STM32F103ZET6。
- [ ] 能找到开发板的 3.3V 和 GND，并会用万用表测量电压。
- [ ] 知道通断档和电阻档要在断电状态下使用。
- [ ] ST-Link 的 SWDIO、SWCLK、GND 已按标签接到开发板。
- [ ] `lsusb` 能看到 ST-Link 或 DAP-Link。
- [ ] USB-TTL 的逻辑电平已经确认。
- [ ] 知道 UART 的 TX/RX 要交叉，并且两边要共地。
- [ ] 已开始填写 [板卡资源约定](./board-zet6-profile.md)。

这些都确认以后，再进入第 0 章搭工程。后面遇到烧录失败、串口乱码或 GPIO 没反应时，也按同样顺序先检查供电和接线，再检查软件配置。

[上一节：前言](./00-preface.md)

[下一章：第 0 章 · 开发环境搭建（ZET6 版）](./00-chapter.md)
