# 第 29 章 · 低功耗设计：先测量，再进入睡眠（SPL 版）

这一章把低功耗拆成三个问题：整机是谁在耗电、MCU 进入哪种模式、唤醒后哪些状态必须恢复。重点放在 STM32F103 的 Sleep、Stop、Standby，以及 Stop 模式下的时钟恢复和 FreeRTOS 时间基准。

低功耗结果必须用测量数据说明。只调用 `WFI` 或 `PWR_EnterSTOPMode()`，不能证明整机功耗已经下降。

## 29.1 先测整机基线

实际电流来自 MCU、传感器、无线模块、LED、电源芯片和板载调试器等所有器件。开发板上的电源 LED、USB-TTL、ST-Link、LDO 静态电流，可能已经高于 MCU 进入 Stop 后的电流。

第一轮测量至少记录：供电电压、供电路径、当前模式、哪些外设上电、平均电流和峰值电流。每次只改变一个条件，例如关闭 OLED、关闭 WiFi、关闭传感器，再比较电流差值。

万用表适合观察平均电流。无线发送、唤醒和稳压器瞬态需要示波器、电流探头或电流分析工具。测量工具本身也可能引入负担电压，特别是在无线模块有较大峰值电流时。

## 29.2 Sleep、Stop、Standby 的区别

STM32F1 常用三种低功耗模式：

| 模式 | CPU | 主时钟/外设 | 唤醒后软件状态 |
|---|---|---|---|
| Sleep | 停止执行 | 大部分时钟继续 | 从睡眠点继续 |
| Stop | 高速时钟停止，稳压器可进低功耗 | 需要恢复系统时钟和相关外设 | 从睡眠点继续 |
| Standby | 大部分内部状态丢失 | 仅保留少量低功耗域 | 更接近一次重新启动 |

Sleep 适合短暂等待中断，恢复成本低。Stop 适合周期采样和较长待机，但唤醒后必须重新建立高速时钟路径。Standby 功耗更低，恢复路径也更接近复位启动，适合更长时间待机。

具体可用唤醒源、寄存器保留范围和电气限制以 STM32F103 对应参考手册为准。

## 29.3 唤醒源要和低功耗模式一起设计

进入低功耗前先确定谁负责唤醒。常见来源包括 RTC Alarm、外部 EXTI、WKUP 引脚，以及目标模式明确支持的其他事件。

例如周期采样节点可以用 RTC 唤醒；按键或门磁可以通过 EXTI 唤醒。UART RX 能否作为可靠唤醒条件，要看当前模式、引脚映射、外设时钟和模块行为，不能只因为运行态 USART 能接收就直接假定 Stop 下也成立。

进入 Stop 前还要清理对应 pending 标志，避免刚进入低功耗就立即被旧事件唤醒。

## 29.4 Stop 前先结束正在进行的事务

进入 Stop 前，先把系统切到一个可恢复状态。至少检查：

- UART 是否还有未发送完的数据；
- DMA 是否仍在搬运缓冲区；
- I2C/SPI 是否处于事务中；
- SD/FatFs 是否需要同步当前写入；
- 外部传感器和无线模块是否需要进入自己的休眠状态；
- GPIO 是否会在休眠期间形成浮空或无意义电流路径。

不要在 I2C 传输到一半、DMA 正在改写缓冲区或日志尚未落盘时直接进入 Stop。低功耗入口本身也要有失败返回，例如外设在规定时间内无法空闲时，本次不进入 Stop。

## 29.5 STM32F103 Stop 唤醒后要恢复时钟

对 STM32F103，Stop 唤醒后代码会从 `PWR_EnterSTOPMode()` 后继续执行，但原来的 72 MHz 时钟树不会自动恢复到进入前状态。项目应复用第 5 章已经验证过的 `SystemClock_Config()`，重新启用 HSE/PLL 并切回目标系统时钟。

一个最小实验可以写成：

```c
void Power_EnterStop(void)
{
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR, ENABLE);

    PrepareWakeupSource();
    Periph_PrepareForStop();

    PWR_EnterSTOPMode(PWR_Regulator_LowPower, PWR_STOPEntry_WFI);

    SystemClock_Config();
    Periph_ResumeAfterStop();
}
```

`Periph_ResumeAfterStop()` 不能只是一个空名字。它应列出项目真正依赖的恢复动作，例如 USART 波特率、I2C、SPI、定时器、DMA 接收路径、传感器供电和无线模块状态。

## 29.6 恢复顺序要能验证

一次 Stop 事务可以按下面顺序检查：

```text
准备唤醒源
  ↓
清 pending / 保存必要状态
  ↓
完成 UART、DMA、I2C、SPI 等事务
  ↓
外部模块进入可恢复状态
  ↓
STOP + WFI
  ↓
唤醒事件发生
  ↓
SystemClock_Config()
  ↓
恢复 SysTick/定时器和外设
  ↓
验证 UART、传感器、总线
```

恢复后至少做一项可观察验证。UART 可以发送固定心跳并检查波特率；定时器可以测周期；I2C 可以重新读取设备 ID；传感器可以做一次有效采样。

如果唤醒后 UART 乱码，先检查系统时钟和 USART 配置，不要直接怀疑字符串发送代码。

## 29.7 FreeRTOS 下不能让普通任务随手进入 Stop

FreeRTOS 使用 SysTick 维护 tick 时，Stop 期间普通 tick 不会按运行态继续推进。一个业务任务直接调用 `PWR_EnterSTOPMode()`，然后假定 `vTaskDelay()`、软件超时和 deadline 都自动保持正确，会破坏时间语义。

项目可以选择：先在裸机最小实验中验证 Stop；只使用 Sleep；或者使用已经理解并测试过的 tickless idle / 电源管理路径，对睡眠期间经过的时间进行补偿。本章不提供一个“通用 FreeRTOS Stop 函数”，因为这一部分必须和 port、tick 源、RTC 以及唤醒策略一起设计。

FreeRTOS 接入低功耗后，还要验证任务超时、软件定时器、Queue 等待和网络重连 deadline 在多次睡醒后仍符合预期。

## 29.8 传感器节点按工作周期设计

一个典型周期可以是：

```text
唤醒
  → 恢复系统时钟
  → 给传感器上电/退出休眠
  → 等待稳定时间
  → 采样
  → 本地记录
  → 必要时联网批量发送
  → 关闭无线和传感器
  → 设置下一次唤醒
  → Stop
```

无线连接往往比一次传感器采样持续更久。低频采样设备可以先本地缓存多份数据，再集中建立一次网络连接发送，减少反复连接的时间和能量开销。

是否值得批量发送要用实测数据判断，包括连接时间、发送峰值、失败重试和本地存储开销。

## 29.9 用周期平均电流做预算

如果一个周期包含运行、无线和睡眠三个阶段，可以计算：

```text
I_avg = (I_run × t_run + I_radio × t_radio + I_sleep × t_sleep) / T
T = t_run + t_radio + t_sleep
```

这里所有电流和持续时间都应来自当前硬件的测量。Stop 模式的一次低电流读数不能代表整机平均功耗，因为无线连接、传感器预热和外设峰值可能占据主要能量。

电池工作时间只能做条件化估算：

```text
理论工作时间(h) ≈ 电池标称容量(mAh) × 可用比例 / I_avg(mA)
```

可用比例还受温度、老化、稳压器压差、电池保护和峰值电流影响。没有这些边界时，不要从一个实验值直接得出“可以运行几个月”。

## 29.10 低功耗前后的外设契约

每个外设写清楚三件事：入睡前做什么、醒来后做什么、怎样验证。例如：

- UART：等待最后一帧发送完成；唤醒后重新确认波特率；发送固定心跳验证。
- I2C/SPI：确保总线空闲、CS 安全；唤醒后按项目需要重新初始化；读取设备 ID 或 ACK 验证。
- 传感器：发送休眠命令或断电；唤醒后等待数据手册规定的稳定时间；读取一次合理数据。
- WiFi/BLE：按模块手册进入休眠或断开；唤醒后走明确的恢复/重连状态机；记录连接错误。
- GPIO：避免浮空和不必要输出；恢复复用功能后再启动相关外设。

这些动作最好收进设备驱动，而不是在 `Power_EnterStop()` 中直接操作几十个寄存器。

## 29.11 测试低功耗要覆盖反复睡醒

至少做以下测试：

1. 正常运行时记录整板平均和峰值电流；
2. 每次只关闭一个外设，记录电流变化；
3. 进入 Sleep，验证唤醒和外设连续工作；
4. 进入 Stop，分别测试 RTC/EXTI 等实际唤醒源；
5. 多次循环睡眠和唤醒，记录失败次数、时钟恢复、UART 错误和传感器异常；
6. 在唤醒、无线发送等峰值阶段观察供电电压是否跌落。

循环次数应覆盖项目需要的可靠性目标。教学实验可以先做数百次快速循环，但不能因为“500 次没失败”就直接推导真实产品寿命。

调试器也会影响结果。测低功耗电流时明确 ST-Link、USB-TTL、USB 和板载 LED 是否仍连接，分别记录“整块开发板”和“最小目标硬件”的结果。

## 29.12 本章完成标准

完成本章后，应能回答：

- 当前整机最大的持续耗电源是谁；
- Sleep、Stop、Standby 中哪一种符合当前唤醒和状态保留需求；
- Stop 前哪些事务必须结束；
- 唤醒后系统时钟和哪些外设需要重新配置；
- FreeRTOS tick 在睡眠期间怎样处理；
- 当前采样周期的平均电流由哪些阶段组成；
- 测量结果使用了什么供电路径和测试工具；
- 多次睡醒以后 UART、传感器、网络和时间基准是否仍然正确。

这些数据都能复现以后，再讨论进一步关闭外设时钟、优化唤醒周期或更换硬件电源方案。

> **上一章**：[第 28 章 · 调试与排错](./28-chapter.md)
>
> **下一章**：[第 30 章 · 从原型到产品](./30-chapter.md)
